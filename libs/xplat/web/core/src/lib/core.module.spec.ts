describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { VgmCoreModule } from './core.module';

describe('VgmCoreModule', () => {
  it('should work', () => {
    expect(new VgmCoreModule(null)).toBeDefined();
  });
});
*/
